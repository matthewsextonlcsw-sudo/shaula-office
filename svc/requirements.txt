# shaula-svc — pinned majors, floating patches (Cloud Run rebuilds weekly).
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
httpx>=0.27,<1
google-auth>=2.29,<3
google-cloud-storage>=2.16,<4
# Site-build enrichment (engine/brain.py, wired via SHAULA_SITE_BRAIN —
# UX audit SH-F5). Vertex only, imported lazily; without the SDK the build
# simply ships the deterministic floor.
google-genai>=1.16,<2

# dev/test
pytest>=8,<9
