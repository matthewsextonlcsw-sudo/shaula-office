// Remotion entry point — registers the project's compositions.
// No PHI, no network at render time; marketing video only.
import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
